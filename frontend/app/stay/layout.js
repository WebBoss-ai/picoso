import './stay.css';

export const metadata = {
  title: 'Picoso Stay — Nature stays for explorers',
  description: 'Discover top stay options for nature lovers. Pay at property.',
};

export default function StayLayout({ children }) {
  return children;
}
